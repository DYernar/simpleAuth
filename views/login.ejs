<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/index.css">
    <title>Login</title>
</head>
<body>
    <div class="container-v2">
        <% if (errors != null && errors.length > 0) { %>
            <ul>
                <% for (var i = 0; i < errors.length; i++) { %> 
                <li class="error-appeared"><%= errors[i] %> </li>
                <% } %> 
            </ul>
        <% } %>  
    </div>
    <div class="container">
            <form class="form" action="/auth/login" method="POST">
                <h3>Login form</h3>
            <input type="text" placeholder="Email" name="email" value="<%if(user!= null){%><%= user.email%><%}%>">
                <br>
                <input type="password" placeholder="Password" name="password">
                <br>
                <button >Login</button>
            </form>
    </div>
    <div class="container">
        <a href="/">Back</a>
    </div>
</body>
</html>